/**
 * Notes App 
 */
export default class Main {
    constructor(config) {
    }

    /**
     * Main start point for the app
     * @param {*} params
     */
    async init(params) {
        console.log("Notes1")
    }
}